<p id="notice"><%= notice %></p>

<h1>Listing Estadia</h1>

<table>
  <thead>
    <tr>
      <th>Cpf</th>
      <th>Numero quarto</th>
      <th>Categoria quarto</th>
      <th>Data inicial</th>
      <th>Data final</th>
      <th>Qtde pessoas</th>
      <th>Valor diaria</th>
      <th>Saldo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @estadia.each do |estadia| %>
		<% calcular(estadia.id) %>
      <tr>
        <td><%= estadia.cpf %></td>
        <td><%= estadia.numero_quarto %></td>
        <td><%= estadia.categoria_quarto_id %></td>
        <td><%= estadia.data_inicial %></td>
        <td><%= estadia.data_final %></td>
        <td><%= estadia.qtde_pessoas %></td>
        <td>R$ <%= estadia.valor_diaria %></td>
		<td>R$ <%if @saldo_total.present? %><%= @saldo_total %><% end %></td>
        <td><%= link_to 'Show', estadia %></td>
        <td><%= link_to 'Edit', edit_estadia_path(estadia) %></td>
        <td><%= link_to 'Destroy', estadia, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Estadia', new_estadia_path %>
